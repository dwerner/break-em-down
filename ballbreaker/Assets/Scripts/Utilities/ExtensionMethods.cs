using System;
using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public static class ExtensionMethods {
   /*
   public static IEnumerator Pulse(this MonoBehaviour mb) {

         var sizes = new List<float>() {
            1.0f,1.1f,1.2f,1.3f,1.2f,1.1f,1.0f
         };

         yield return mb.StartCoroutine(mb.Pulse(sizes));
         
   }

   public static IEnumerator Pulse(this MonoBehaviour mb, IEnumerable<float> sizes) {

      var initialScale = mb.transform.localScale;

      foreach (var i in sizes){
         mb.transform.localScale = initialScale * i;
         yield return null;
      }

      yield return null;
   }
   */
}

